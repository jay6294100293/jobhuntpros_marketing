# TTS Setup — OBSOLETE

This file describes Google Cloud TTS setup, which has been replaced.

**Current TTS:** Microsoft Edge TTS (`edge-tts` Python library, AndrewNeural voice).
- Free — no API key, no Google Cloud account required.
- Automatic gTTS fallback if Edge TTS fails.
- Speed: +5% for marketing pacing.

No setup needed. Edge TTS works out of the box when `edge-tts` is installed via `requirements.txt`.

See `docs/SETUP_INSTRUCTIONS.md` for the complete setup guide.
